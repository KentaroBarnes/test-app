Install dependencies with:
`npm install`

Run server on port 5173 with:
`npm run dev`

go to the `/login` route
click on the button and log in with the credentials you created earlier

You should then be redirected to the server to log in.

redirected back to the client app after with access tokens
